"""The role MCP servers over the StateSet iCommerce engine: the same ~20-tool surface
the Messages API host and the Agent SDK console use, through the same executor and
gates — not the engine's own 900+-tool server."""
