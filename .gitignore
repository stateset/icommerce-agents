.venv/
__pycache__/
*.db
*.db-shm
*.db-wal
node_modules/
.next/
.env
.superpowers/
# This project installs through requirements*.txt; `uv run` may generate a local lock.
uv.lock

# Next 16 auto-generates these under each web app during `next build`/`next dev`;
# content is upstream Next's, not this repo's.
web/*/AGENTS.md
web/*/CLAUDE.md
